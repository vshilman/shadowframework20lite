


GLShaderTexture is a texture rendered with shaders.

		---> I don't have a correct texture management process to use.

		This is a Filtered Texture.... that's all.
		
		---->Need to define filtered Textures.
		
		
GLGPUTexture: a generic texture drawn on the GPU.
GLGPUTexture: a generic texture drawn on the GPU.




1) Need to be able to assign more frameBuffers 
	need to be able to assign also fragmentColor[i] to specify render target
	
2) Need to be able to render an SFBitmap
