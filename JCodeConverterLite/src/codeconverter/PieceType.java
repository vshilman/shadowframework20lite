package codeconverter;

/**
 * An attribute which can be assigned to a Piece Type
 * 
 * @author Alessandro
 */
public enum PieceType {
	NAME,
	VARIABLE,
	DATA,
	MODIFIER,
	KEYWORD,
	VALUE,
	CALL,
	TYPE,
	SEQUENCE,
	METHOD_VARIABLES,
	STRUCTURE,
	UNCLASSIFIED,
	EXPRESSION,
	PATTERN,
	IGNORED;
}
